---
pageable: true
per_page: 10
---

<% page_articles.each_with_index do |article, i| %>
    <h2><%= link_to article.title, article %></h2>
    <p class="text-success"><%= article.date.strftime('%Y年%m月%d日') %></p>
    <%= article.summary %>
    <%= link_to 'Read More', article.url, class: "btn btn-primary btn-xs" %>
<% end %>

<% if paginate %>
<ul class="pager">
  <% if prev_page %>
    <li><%= link_to 'Previous', prev_page %></li>
  <% end %>
  <% if next_page %>
      <li><%= link_to 'Next', next_page %></li>
  <% end %>
</ul>
<% end %>
